#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(int argc, char** argv) {
    int termos = 0;

    printf("Digite o numero de termos: \n");
    scanf("%d", termos);

    int i = 0;
    int cont = 2;
    int num1,num2,num3;
    while (i < termos) {
        i++;

        cont++;
        
    }

    return (0);
}

